# Wallet private key
PRIVATE_KEY=your_wallet_private_key

# RPC URL for testnet (e.g. Alchemy, Infura, or QuickNode)
RPC_URL=https://goerli.infura.io/v3/your_project_id

# Deployed addresses (useful if reusing contracts)
NFT_ADDRESS=deployed_nft_contract_address
REWARD_TOKEN_ADDRESS=deployed_reward_token_contract_address
